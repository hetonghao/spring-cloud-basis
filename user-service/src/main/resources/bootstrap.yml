server:
  port: 9999
spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8805
      profile: default
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl